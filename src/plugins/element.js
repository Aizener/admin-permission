import Vue from 'vue'
import {
  Container,
  Header,
  Aside,
  Main,
  Button,
  Menu,
  MenuItem,
  Submenu,
  Breadcrumb,
  BreadcrumbItem,
  Form,
  FormItem,
  Table,
  TableColumn,
  Input,
  Icon,
  Pagination,
  Loading
} from 'element-ui'

Vue
  .use(Container)
  .use(Header)
  .use(Aside)
  .use(Main)
  .use(Button)
  .use(Menu)
  .use(MenuItem)
  .use(Submenu)
  .use(Breadcrumb)
  .use(BreadcrumbItem)
  .use(Form)
  .use(FormItem)
  .use(Table)
  .use(TableColumn)
  .use(Input)
  .use(Icon)
  .use(Pagination)
  .use(Loading)